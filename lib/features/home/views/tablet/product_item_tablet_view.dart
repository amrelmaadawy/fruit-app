import 'package:flutter/material.dart';

class ProductItemTabletView extends StatelessWidget {
  const ProductItemTabletView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}