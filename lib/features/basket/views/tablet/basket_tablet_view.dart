import 'package:flutter/material.dart';

class BasketTabletView extends StatelessWidget {
  const BasketTabletView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}