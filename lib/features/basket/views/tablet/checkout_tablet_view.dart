import 'package:flutter/material.dart';

class CheckoutTabletView extends StatelessWidget {
  const CheckoutTabletView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}