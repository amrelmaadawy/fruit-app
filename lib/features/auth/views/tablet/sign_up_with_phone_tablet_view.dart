import 'package:flutter/material.dart';

class SignUpWithPhoneTabletView extends StatelessWidget {
  const SignUpWithPhoneTabletView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}